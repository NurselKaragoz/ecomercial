const initialState = {
  productList: [],
  totalProductCount: [],
  pageCount: [],
  activePage: [],
  fetchState: [],
};
const productReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default productReducer;
