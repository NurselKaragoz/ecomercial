const initialState = {
  name: [],
  email: [],
  password: [],
  role: [],
};
const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userReducer;
